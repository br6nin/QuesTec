# Ativa o motor de reescrita de URLs
RewriteEngine On

# 1. Base: Estamos na pasta /blog/
# Se o seu blog está na raiz do site, apague esta linha
# RewriteBase /blog/ 

# 2. Condição: Verifica se a requisição não é um arquivo ou diretório real (ex: /blog/styles.css)
# Se não for um arquivo que existe, passa para a próxima regra (RewriteRule)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# 3. Regra: Se a URL não existe, reescreve ela internamente
# ^(.*)$ captura o que vier na URL (o slug).
# post.html?slug=$1 manda o que foi capturado ($1) para o post.html.
# [L,QSA] são flags de controle (Last e Query String Append).
RewriteRule ^(.*)$ post.html?slug=$1 [L,QSA]